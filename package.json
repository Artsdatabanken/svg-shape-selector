{
  "name": "svg-shape-selector",
  "version": "0.1.3",
  "private": false,
  "homepage": "https://github.com/artsdatabanken/svg-shape-selector",
  "peerDependencies": {
    "react": "^16.8.0"
  },
  "dependencies": {},
  "scripts": {
    "publish:npm": "rm -rf dist && mkdir dist &&  babel src/components -d dist --copy-files"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@babel/cli": "^7.7.0",
    "@babel/core": "^7.7.4",
    "@babel/preset-react": "^7.7.0"
  },
  "babel": {
    "presets": [
      "@babel/preset-react"
    ]
  }
}
